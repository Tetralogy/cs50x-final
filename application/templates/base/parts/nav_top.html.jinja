<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Todo App</a>
    <div class="d-flex">{% include 'profile/parts/avatar.html.jinja' %}</div>
    <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation"
    >
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            {% if current_user.is_authenticated %}
            <div class="form-check form-switch">
  <input class="form-check-input" type="checkbox" role="switch" id="debug-switch"
        hx-put="/debug"
        {% if current_user.debug %} checked {% endif %}>
  <label class="form-check-label" for="flexSwitchCheckChecked">Debug</label>
</div>

                <div class="d-flex">
                    {% include 'profile/parts/avatar.html.jinja' %}
                </div>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('account.profile') }}"
                        >Profile</a
                    >
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('main.index') }}"
                        >Home</a
                    >
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/home/floors/setup"
                        >Edit Floors</a
                    >
                </li>
                <li class="nav-item">
                    <a
                        class="nav-link"
                        href="/home/rooms/setup/{{ current_user.active_home.active_floor_id }}"
                        >Edit Rooms</a
                    >
                </li>
                <li class="nav-item">
                    <a
                        class="nav-link"
                        href="/map/{{ current_user.active_home.active_floor_id }}"
                        >Map</a
                    >
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('main.tasklist') }}"
                        >Tasks</a
                    >
                </li>

                <li>
                    <a class="nav-link" href="{{ url_for('auth.logout') }}"
                        >Logout</a
                    >
                </li>
                <li>
                    <a class="nav-link" href="{{ url_for('auth.password') }}"
                        >Password Reset</a
                    >
                </li>
                <div>
                    <h4 class="text-primary">
                        Welcome, {{ current_user.username }}! Email:
                        {{ current_user.email }} User ID: {{ current_user.id }}
                    </h4>
                </div>
            {% else %}
                <li>
                    <a class="nav-link" href="{{ url_for('auth.register') }}"
                        >Register</a
                    >
                </li>
                <li>
                    <a class="nav-link" href="{{ url_for('auth.login') }}"
                        >Login</a
                    >
                </li>
            {% endif %}
            <li>
                <a class="nav-link" href="{{ url_for('main.sitemap') }}"
                    >Sitemap</a
                >
            </li>
        </ul>
    </div>
</nav>
