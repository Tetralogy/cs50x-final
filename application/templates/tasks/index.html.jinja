{% extends "base/layout.html" %}
{% block title %}
    Tasks
{% endblock %}

{% block main %}
#: list all tasks
#todo: list all rooms
#todo: remove 'zones' concept
#todo: list all photos
#todo: heigharchial view of tasks
#todo: prioritize tasks
<h1>
     current_user.active_home 
    {{ current_user.active_home }}
</h1>
<h1>List of all lists for the current home:</h1>

    <div
                class="row g-0 row-cols-4 container-fluid bg-dark border border-success border-5 rounded-bottom-4 p-1"
                id="task-list-loader"
                hx-get="/show_list/"
                hx-vals='{"parent": "{{ current_user.active_home }}", "sublevels": "100"}'
                hx-trigger="load once"
                hx-swap="outerHTML"
                hx-indicator="#updating-indicator"
            >
                <input id="dummy-placeholder" type="hidden" name="items" />
                
    </div>

    <h1>This is a view of all tasks for the user:</h1>

    {#<div
                class="row g-0 row-cols-4 container-fluid bg-dark border border-success border-5 rounded-bottom-4 p-1"
                id="task-list-loader"
                hx-get="/show_list/"
                hx-vals='{"list_type": "Task", "combine": "True", "parent": "{{ current_user.active_home }}"}'
                hx-trigger="load once"
                hx-swap="outerHTML"
                hx-indicator="#updating-indicator"
            >
                <input id="dummy-placeholder" type="hidden" name="items" />
                
    </div>
    <h1>List of all homes for the user:</h1>
    <div
                class="row g-0 row-cols-4 container-fluid bg-dark border border-success border-5 rounded-bottom-4 p-1"
                id="home-list-loader"
                hx-get="/show_list/"
                hx-vals='{"list_type": "Home", "combine": "True",}'
                hx-trigger="load once"
                hx-swap="outerHTML"
                hx-indicator="#updating-indicator"
            >
                <input id="dummy-placeholder" type="hidden" name="items" />
                
    </div>#}

    <h1>List of all rooms for the user:</h1>
    <h1>List of all photos for the user:</h1>



{% endblock %}
