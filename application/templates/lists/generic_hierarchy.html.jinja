        <div
            id="list-updater-{{ list_obj.id }}"

                class="row g-0 row-cols-1 border border-primary border-5
                rounded-4 p-1 sortable" 
                style="min-width: 20em; max-width: 40em;
                display: block; margin-left: auto; margin-right: auto;"


            hx-put="/update_list_order/{{ list_obj.id }}"
            hx-include="[name='items']"
            hx-trigger="end, remove, add delay:1s"
            hx-indicator="#updating-indicator"
            data-model="{{ list_obj.list_type }}"
            data-list_id="{{ list_obj.id }}"
        >
        >
            <div class="accordion" id="accordionExample">
    <div class="accordion-item">

        <h2 class="accordion-header">

    <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#list-updater-{{ list_obj.id }}" aria-expanded="true" aria-controls="multiCollapseExample1 multiCollapseExample2">hide/show sublevel: {{ sublevel }} sublevel_limit: {{ sublevel_limit }} text-hierarchy</button>
        </h2>
            {% if reversed %}
                {% for entry in list_obj.entries | sort(attribute='order')| reverse %}
                    {% include 'lists/model/generic_text.html.jinja' %}
                {% endfor %}
            {% else %}
                {% for entry in list_obj.entries | sort(attribute='order') %}
                    {% include 'lists/model/generic_text.html.jinja' %}
                {% endfor %}
            {% endif %}

            {% if not list_obj %}
                {% if view == "quicknote" %}
                    {% include 'lists/model/generic_text.html.jinja' %}
                {% else %}
                    <h1>Failed to load list</h1>
                {% endif %}
            {% endif %}
        </div>
            </div>
    </div>