<p> 
    Please quickly add task drafts for this room. You can add things that need
    to be cleaned or organized or maintained or upgraded.
</p>

<div class="container-fluid">
            <div
                class="row g-0 row-cols-4 container-fluid bg-dark border border-success border-5 rounded-bottom-4 p-1"
                id="task-list-loader"
                hx-get="/show_list/"
                hx-vals='{"list_model": "Photo", "view_override":"sortphotos"}'
                hx-trigger="load once"
                hx-swap="outerHTML"
                hx-indicator="#updating-indicator"
            >
                <input id="dummy-placeholder" type="hidden" name="items" />
                
            </div>

</div>

<p>{{ current_user.active_home.active_room.name }} active photo {{ current_user.active_home.active_room.current_cover_photo_id }}:</p>


<div
id="photo-loader"
hx-get="/show_photo_entry/{{ current_user.active_home.active_room.current_cover_photo_id }}"
hx-trigger="load once"
hx-swap="outerHTML"
hx-indicator="#updating-indicator"
>
</div>