<p>
    #FIXME PHOTO Upload INSTRUCTIONS: Please take a panorama picture of the
    room. Stand in the center of the room and take either 1 wide angle picture
    from a corner or 2 pictures from the middle of the room.
</p>
<form hx-post="/create/Photo" enctype="multipart/form-data" hx-target="#test-photo-list-loader"
        hx-swap="afterend" hx-on::after-request="this.reset()">
    <input
        type="file"
        accept="image/*"
        capture="environment"
        name="room_photo"
        
    />

    <button type="submit" class="btn btn-primary">Upload photo</button>
</form>

<p>temporary for testing: show photos here:</p>

<div id="test-photo-list-loader"
    hx-get="/show_list/"
    hx-vals='{"list_type": "Photo"}'
    hx-trigger="load once"
></div>
